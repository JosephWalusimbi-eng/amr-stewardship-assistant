# Model weights — never commit these to git
model/*.gguf
model/*.bin
model/*.safetensors
model/*.complete

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Local profiler output
submission.json
audit.json

# Prefix-cache pickles — regenerated by cache_warmup.py, not source
cache/*.pkl

# Run logs
*.log

# Raw corpus PDFs — copyrighted source docs; the prebuilt rag/index.pkl
# ships instead so the app runs without regenerating it
corpus/*.pdf

# Windows download metadata
*Zone.Identifier*
DEMO_SCRIPT_v3.md
DEVPOST_DRAFT.md

how to run.txt

# Scratch scripts used while building the dataset pipeline
.scratch/

# Generated dataset artefacts (regenerate with scripts/, keep raw/ out of git
# until the clinical hand-check is done)
raw/
dataset/
